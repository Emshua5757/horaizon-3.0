pub struct Lexer;

impl Lexer {
    pub fn parse_imports(_content: &str, _language: &str) -> Vec<String> {
        // TODO: regex match import lines based on language extension
        vec![]
    }
}
