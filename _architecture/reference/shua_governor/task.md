# SHUA Governor: Development Task Tracker

- `[x]` **Phase 0: Blueprint Alignment**
  - `[x]` Modify `blueprints/MASTERPLAN.md` to reference Rust `shua_governor`
  - `[x]` Modify `blueprints/core/CORE_AUTOMATION.md` to align with Rust design
  - `[x]` Modify `blueprints/technical/TECH_INFRASTRUCTURE.md` to align cgroups Python mock references with Rust concepts
- `[ ]` **Phase 1: Base Gateway & Hyper Reverse Proxy**
  - `[ ]` Implement cross-platform virtual cgroups platform detection and setup
  - `[ ]` Scaffold core Axum HTTP server on port 3000
  - `[ ]` Route `/health` check handler local response
  - `[ ]` Implement async reverse-proxy forwarding pipeline for `/api/diary/*` calls to Node.js backend (`http://127.0.0.1:3001`) with zero-copy stream piping
  - `[ ]` Configure Express backend configurations to listen on port `3001`
- `[ ]` **Phase 2: Process Telemetry & cgroups Control**
  - `[ ]` Set up filesystem watchers/writers for `/sys/fs/cgroup/shua/` interface
  - `[ ]` Implement Windows platform fallback directory tracking mock structures at `C:\temp\shua\`
  - `[ ]` Spawn microservice subprocesses via `tokio::process::Command` and enroll PIDs into `cgroup.procs`
  - `[ ]` Integrate `sysinfo` to query physical memory footprint and process status
- `[ ]` **Phase 3: Server-Driven UI (SDUI) Compiler**
  - `[ ]` Model `SduiNode` layout structures matching the widget schema contracts
  - `[ ]` Implement dynamic `/api/dashboard` JSON endpoint serving HBP `GridView` and module `Card` layouts
  - `[ ]` Implement Lazy Cache Loader with VFS Metadata Validation for hot-reloading `dashboard.json` blueprints without daemon restarts
  - `[ ]` Add toggling controls via `POST /api/governor/toggle/:id` to freeze/thaw cgroup processes
  - `[ ]` Integrate `rmp-serde` for binary MsgPack optimization framing
- `[ ]` **Phase 4: Testing & Hardening**
  - `[ ]` Write automated validation tests to verify structural integrity and forwarding handlers
