# AUTO-GENERATED by sync_contracts at 2026-08-05 13:24:39 UTC — DO NOT EDIT
# Source: tools/sync_contracts/schema/hbp_operations.toml
# HBP v2 — horAIzon Binary Protocol v2 — data-only, bidirectional RPC over WebSocket + MessagePack

from __future__ import annotations


from dataclasses import dataclass, field
from typing import Optional, Any
from .hbp_enums import *

# Single parameter signature representation
@dataclass
class ParamDto:
    name: str
    type_name: str
    is_optional: bool

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ParamDto':
        return cls(
            name=d['name']
            type_name=d['type_name']
            is_optional=d['is_optional']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'name': self.name,
            'type_name': self.type_name,
            'is_optional': self.is_optional,
        }

# Structured node representation for AST code topology
@dataclass
class GraphNode:
    id: str
    kind: str
    qualified_name: str
    file: str
    line: int
    params: list[ParamDto] = field(default_factory=list)
    return_type: str
    complexity: int
    side_effects: list[str] = field(default_factory=list)
    intent: str
    loc: int
    fan_in: int
    fan_out: int
    risk_score: float
    is_orphan: bool
    exceeds_param_threshold: bool
    exceeds_complexity_threshold: bool
    exceeds_loc_threshold: bool

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'GraphNode':
        return cls(
            id=d['id']
            kind=d['kind']
            qualified_name=d['qualified_name']
            file=d['file']
            line=d['line']
            params=[ParamDto.from_dict(e) for e in (d.get('params') or [])]
            return_type=d['return_type']
            complexity=d['complexity']
            side_effects=d['side_effects']
            intent=d['intent']
            loc=d['loc']
            fan_in=d['fan_in']
            fan_out=d['fan_out']
            risk_score=d['risk_score']
            is_orphan=d['is_orphan']
            exceeds_param_threshold=d['exceeds_param_threshold']
            exceeds_complexity_threshold=d['exceeds_complexity_threshold']
            exceeds_loc_threshold=d['exceeds_loc_threshold']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'id': self.id,
            'kind': self.kind,
            'qualified_name': self.qualified_name,
            'file': self.file,
            'line': self.line,
            'params': [e.to_dict() for e in self.params],
            'return_type': self.return_type,
            'complexity': self.complexity,
            'side_effects': self.side_effects,
            'intent': self.intent,
            'loc': self.loc,
            'fan_in': self.fan_in,
            'fan_out': self.fan_out,
            'risk_score': self.risk_score,
            'is_orphan': self.is_orphan,
            'exceeds_param_threshold': self.exceeds_param_threshold,
            'exceeds_complexity_threshold': self.exceeds_complexity_threshold,
            'exceeds_loc_threshold': self.exceeds_loc_threshold,
        }

# Dependency call/import edge between symbols
@dataclass
class GraphEdge:
    from: str
    to: str
    relation: str

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'GraphEdge':
        return cls(
            from=d['from']
            to=d['to']
            relation=d['relation']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'from': self.from,
            'to': self.to,
            'relation': self.relation,
        }

# Full AST topology graph payload
@dataclass
class TopologyExportResponse:
    nodes: list[GraphNode] = field(default_factory=list)
    edges: list[GraphEdge] = field(default_factory=list)

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'TopologyExportResponse':
        return cls(
            nodes=[GraphNode.from_dict(e) for e in (d.get('nodes') or [])]
            edges=[GraphEdge.from_dict(e) for e in (d.get('edges') or [])]
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'nodes': [e.to_dict() for e in self.nodes],
            'edges': [e.to_dict() for e in self.edges],
        }

# Incremental code delta push on file change
@dataclass
class TopologyDeltaEvent:
    file_path: str
    change_type: str
    affected_node_ids: list[str] = field(default_factory=list)

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'TopologyDeltaEvent':
        return cls(
            file_path=d['file_path']
            change_type=d['change_type']
            affected_node_ids=d['affected_node_ids']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'file_path': self.file_path,
            'change_type': self.change_type,
            'affected_node_ids': self.affected_node_ids,
        }

# Standardized structured error payload for HBP v2 responses
@dataclass
class HbpError:
    # Standard error code (e.g. 400 bad request, 404 not found, 500 internal)
    code: int
    # Error category enum code
    category: ErrorCategory
    # Human-readable error description
    message: str
    # Optional context details key-value map
    details: Optional[dict[str, str]] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'HbpError':
        return cls(
            code=d['code']
            category=ErrorCategory(d['category'])
            message=d['message']
            details=d.get('details')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'code': self.code,
            'category': self.category.value,
            'message': self.message,
            'details': self.details,
        }

# Universal HBP v2 message envelope — every message uses this outer shape
@dataclass
class HbpFrame:
    # Protocol version, always 2
    v: int
    # Message type code
    t: MessageType
    # Transaction ID — UUID v4. RESPONSE echoes REQUEST id. EVENT generates its own.
    id: str
    # Module namespace e.g. shua.resume
    mod: str
    # Operation name e.g. compile
    op: str
    # Timestamp of creation (UTC)
    ts: int
    # Payload bytes — msgpack-encoded operation body. Empty string for PING/PONG.
    p: str
    # null on success. Structured error object on failure.
    err: Optional[HbpError] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'HbpFrame':
        return cls(
            v=d['v']
            t=MessageType(d['t'])
            id=d['id']
            mod=d['mod']
            op=d['op']
            ts=d['ts']
            p=d['p']
            err=HbpError.from_dict(d['err']) if d.get('err') is not None else None
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'v': self.v,
            't': self.t.value,
            'id': self.id,
            'mod': self.mod,
            'op': self.op,
            'ts': self.ts,
            'p': self.p,
            'err': self.err.to_dict() if self.err is not None else None,
        }

# Standardized pagination metadata wrapper
@dataclass
class PaginationMeta:
    # Total matching items count
    total_items: int
    # True if additional pages exist
    has_more: bool
    # Current page index (0-indexed)
    page: int
    # Items per page
    page_size: int

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'PaginationMeta':
        return cls(
            total_items=d['total_items']
            has_more=d['has_more']
            page=d['page']
            page_size=d['page_size']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'total_items': self.total_items,
            'has_more': self.has_more,
            'page': self.page,
            'page_size': self.page_size,
        }

# Server-pushed sentiment analysis event
@dataclass
class SentimentEvent:
    entry_id: str
    score: float
    label: str

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'SentimentEvent':
        return cls(
            entry_id=d['entry_id']
            score=d['score']
            label=d['label']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'entry_id': self.entry_id,
            'score': self.score,
            'label': self.label,
        }

# Server-pushed real-time entry update event for multi-device optimistic concurrency
@dataclass
class EntryUpdatedEvent:
    entry_id: str
    block_id: str
    version: int

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'EntryUpdatedEvent':
        return cls(
            entry_id=d['entry_id']
            block_id=d['block_id']
            version=d['version']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'entry_id': self.entry_id,
            'block_id': self.block_id,
            'version': self.version,
        }

# Module process description and live telemetry returned in governor.status
@dataclass
class ModuleEntry:
    # Module namespace string e.g. shua.resume
    name: str
    # Current process state
    state: ModuleState
    # OS Process ID if running or sleeping
    pid: Optional[int] = None
    # Current CPU load percentage
    cpu_percent: Optional[float] = None
    # Current RSS/cgroup memory usage in megabytes
    ram_mb: Optional[float] = None
    # Configured memory ceiling limit in megabytes
    ram_limit_mb: Optional[int] = None
    # Total process uptime in seconds
    uptime_s: Optional[int] = None
    # True if module process health check is passing
    health_ok: bool
    # Number of auto-restarts following crashes
    restart_count: int
    # Most recent crash or exit reason description
    last_error: Optional[str] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ModuleEntry':
        return cls(
            name=d['name']
            state=ModuleState(d['state'])
            pid=d.get('pid')
            cpu_percent=d.get('cpu_percent')
            ram_mb=d.get('ram_mb')
            ram_limit_mb=d.get('ram_limit_mb')
            uptime_s=d.get('uptime_s')
            health_ok=d['health_ok']
            restart_count=d['restart_count']
            last_error=d.get('last_error')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'name': self.name,
            'state': self.state.value,
            'pid': self.pid,
            'cpu_percent': self.cpu_percent,
            'ram_mb': self.ram_mb,
            'ram_limit_mb': self.ram_limit_mb,
            'uptime_s': self.uptime_s,
            'health_ok': self.health_ok,
            'restart_count': self.restart_count,
            'last_error': self.last_error,
        }

# Current Ollama subsystem state
@dataclass
class OllamaInfo:
    # Currently loaded model name or null
    loaded_model: Optional[str] = None
    # VRAM/RAM footprint of loaded model in MB
    ram_mb: Optional[float] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'OllamaInfo':
        return cls(
            loaded_model=d.get('loaded_model')
            ram_mb=d.get('ram_mb')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'loaded_model': self.loaded_model,
            'ram_mb': self.ram_mb,
        }

# Response payload for governor.status
@dataclass
class GovernorStatusResponse:
    # Array of all registered module states
    modules: list[ModuleEntry] = field(default_factory=list)
    # Ollama lifecycle state
    ollama: OllamaInfo

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'GovernorStatusResponse':
        return cls(
            modules=[ModuleEntry.from_dict(e) for e in (d.get('modules') or [])]
            ollama=OllamaInfo.from_dict(d['ollama'])
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'modules': [e.to_dict() for e in self.modules],
            'ollama': self.ollama.to_dict() if self.ollama is not None else None,
        }

# Request payload for governor.ollama.load
@dataclass
class OllamaLoadRequest:
    # Ollama model name e.g. qwen2.5:1.5b
    model: str

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'OllamaLoadRequest':
        return cls(
            model=d['model']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'model': self.model,
        }

@dataclass
class OllamaLoadResponse:
    loaded_model: str
    ram_mb: float
    duration_ms: int

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'OllamaLoadResponse':
        return cls(
            loaded_model=d['loaded_model']
            ram_mb=d['ram_mb']
            duration_ms=d['duration_ms']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'loaded_model': self.loaded_model,
            'ram_mb': self.ram_mb,
            'duration_ms': self.duration_ms,
        }

@dataclass
class ModuleWakeRequest:
    # Module namespace to wake e.g. shua.resume
    module: str

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ModuleWakeRequest':
        return cls(
            module=d['module']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'module': self.module,
        }

@dataclass
class AiRouteRequest:
    # User input prompt text
    prompt: str
    # Optional module domain hint e.g. diary
    context_hint: Optional[str] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'AiRouteRequest':
        return cls(
            prompt=d['prompt']
            context_hint=d.get('context_hint')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'prompt': self.prompt,
            'context_hint': self.context_hint,
        }

@dataclass
class AiRouteResponse:
    model_used: str
    intent: IntentClass
    reply: str
    duration_ms: int
    # List of agent loop turn step records
    steps: list[AgentLoopStepDto] = field(default_factory=list)

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'AiRouteResponse':
        return cls(
            model_used=d['model_used']
            intent=IntentClass(d['intent'])
            reply=d['reply']
            duration_ms=d['duration_ms']
            steps=[AgentLoopStepDto.from_dict(e) for e in (d.get('steps') or [])]
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'model_used': self.model_used,
            'intent': self.intent.value,
            'reply': self.reply,
            'duration_ms': self.duration_ms,
            'steps': [e.to_dict() for e in self.steps],
        }

# Record of a single tool call executed within an agent loop step
@dataclass
class ToolCallStepDto:
    # Name of the executed MCP tool
    tool_name: str
    # Truncated string summary of the tool execution output
    result_summary: str
    # True if tool executed successfully
    success: bool

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ToolCallStepDto':
        return cls(
            tool_name=d['tool_name']
            result_summary=d['result_summary']
            success=d['success']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'tool_name': self.tool_name,
            'result_summary': self.result_summary,
            'success': self.success,
        }

# Record of a single turn iteration in the N-turn agent loop
@dataclass
class AgentLoopStepDto:
    # Turn iteration index (1..5)
    turn: int
    # Step category (tool_execution, inline_tool_execution, nudge, final_answer)
    step_type: str
    # Raw LLM output text for this turn
    model_content: str
    # List of tool calls executed during this turn
    tool_calls: list[ToolCallStepDto] = field(default_factory=list)

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'AgentLoopStepDto':
        return cls(
            turn=d['turn']
            step_type=d['step_type']
            model_content=d['model_content']
            tool_calls=[ToolCallStepDto.from_dict(e) for e in (d.get('tool_calls') or [])]
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'turn': self.turn,
            'step_type': self.step_type,
            'model_content': self.model_content,
            'tool_calls': [e.to_dict() for e in self.tool_calls],
        }

# System configuration settings payload returned/updated via governor.config.*
@dataclass
class GovernorConfigDto:
    # HBP WebSocket broker server port (default 7700)
    port: int
    # Global log verbosity level (trace, debug, info, warn, error)
    log_level: str
    # System timezone string (e.g. Asia/Manila)
    timezone: str
    # Optional laptop node URL for heavy AI offloading
    offload_device_url: Optional[str] = None
    # Ollama model RAM ceiling cap in megabytes
    ollama_ram_cap_mb: int
    # Nightly 02:00 AM maintenance dream loop toggle
    dream_loop_enabled: bool
    # Dream loop cron schedule expression
    dream_loop_cron: str
    # SQLite log database retention period in days
    log_retention_days: int

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'GovernorConfigDto':
        return cls(
            port=d['port']
            log_level=d['log_level']
            timezone=d['timezone']
            offload_device_url=d.get('offload_device_url')
            ollama_ram_cap_mb=d['ollama_ram_cap_mb']
            dream_loop_enabled=d['dream_loop_enabled']
            dream_loop_cron=d['dream_loop_cron']
            log_retention_days=d['log_retention_days']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'port': self.port,
            'log_level': self.log_level,
            'timezone': self.timezone,
            'offload_device_url': self.offload_device_url,
            'ollama_ram_cap_mb': self.ollama_ram_cap_mb,
            'dream_loop_enabled': self.dream_loop_enabled,
            'dream_loop_cron': self.dream_loop_cron,
            'log_retention_days': self.log_retention_days,
        }

# Universal HBP Stream Frame container for arbitrary data/media streams
@dataclass
class StreamFrameDto:
    # Media stream classification (LlmToken, AudioPcm, VideoNal, etc.)
    media_type: StreamMediaType
    # Monotonic chunk sequence index (0, 1, 2...)
    sequence_num: int
    # UTF-8 text delta string or Base64/MsgPack binary data chunk
    chunk_data: str
    # True if this chunk terminates the active stream
    is_last: bool

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'StreamFrameDto':
        return cls(
            media_type=StreamMediaType(d['media_type'])
            sequence_num=d['sequence_num']
            chunk_data=d['chunk_data']
            is_last=d['is_last']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'media_type': self.media_type.value,
            'sequence_num': self.sequence_num,
            'chunk_data': self.chunk_data,
            'is_last': self.is_last,
        }

# Client WebSocket subscription filter for live log events
@dataclass
class LogFilter:
    # Minimum log level (1=TRACE..5=ERROR)
    min_level: Optional[int] = None
    # List of module namespaces to filter
    modules: Optional[list[str]] = field(default_factory=list)
    # Tag bitmask filter
    tag_mask: Optional[int] = None
    # List of subsystem names to exclude (e.g. governor_heartbeat)
    exclude_subsystems: Optional[list[str]] = field(default_factory=list)

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'LogFilter':
        return cls(
            min_level=d.get('min_level')
            modules=d['modules']
            tag_mask=d.get('tag_mask')
            exclude_subsystems=d['exclude_subsystems']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'min_level': self.min_level,
            'modules': self.modules,
            'tag_mask': self.tag_mask,
            'exclude_subsystems': self.exclude_subsystems,
        }

# Centralized log event entry payload
@dataclass
class LogEntryDto:
    # Timestamp of creation (UTC)
    ts: int
    # Log level (1=TRACE..5=ERROR)
    level: int
    # Module ID
    module: int
    # Subsystem component name
    subsystem: str
    # Log message text
    msg: str
    # Tag bitmask
    tags: int
    # Optional structured telemetry key-value pairs
    telemetry: Optional[map] = None
    # Human-readable module identifier (e.g. shua.resume). Canonical string for Flutter attribution.
    module_name: Optional[str] = None
    # Optional transaction trace ID
    trace_id: Optional[str] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'LogEntryDto':
        return cls(
            ts=d['ts']
            level=d['level']
            module=d['module']
            subsystem=d['subsystem']
            msg=d['msg']
            tags=d['tags']
            telemetry=d.get('telemetry')
            module_name=d.get('module_name')
            trace_id=d.get('trace_id')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'ts': self.ts,
            'level': self.level,
            'module': self.module,
            'subsystem': self.subsystem,
            'msg': self.msg,
            'tags': self.tags,
            'telemetry': self.telemetry,
            'module_name': self.module_name,
            'trace_id': self.trace_id,
        }

# Request payload for governor.logs.query
@dataclass
class LogQueryRequestDto:
    min_level: Optional[int] = None
    module: Optional[int] = None
    subsystem: Optional[str] = None
    start_ts: Optional[int] = None
    end_ts: Optional[int] = None
    trace_id: Optional[str] = None
    limit: Optional[int] = None
    offset: Optional[int] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'LogQueryRequestDto':
        return cls(
            min_level=d.get('min_level')
            module=d.get('module')
            subsystem=d.get('subsystem')
            start_ts=d.get('start_ts')
            end_ts=d.get('end_ts')
            trace_id=d.get('trace_id')
            limit=d.get('limit')
            offset=d.get('offset')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'min_level': self.min_level,
            'module': self.module,
            'subsystem': self.subsystem,
            'start_ts': self.start_ts,
            'end_ts': self.end_ts,
            'trace_id': self.trace_id,
            'limit': self.limit,
            'offset': self.offset,
        }

# Response payload for governor.logs.query
@dataclass
class LogQueryResponseDto:
    total: int
    entries: list[LogEntryDto] = field(default_factory=list)

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'LogQueryResponseDto':
        return cls(
            total=d['total']
            entries=[LogEntryDto.from_dict(e) for e in (d.get('entries') or [])]
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'total': self.total,
            'entries': [e.to_dict() for e in self.entries],
        }

# Request payload for resume.compile
@dataclass
class ResumeCompileRequest:
    # ID of resume matrix to compile
    matrix_id: str
    # Typst template name
    template: str
    # Optional job description text for AI tailoring
    job_desc: Optional[str] = None
    # Apply AI keyword tailoring filter
    tailor: bool
    # Apply full Ollama enhancement
    ai_enhance: bool

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ResumeCompileRequest':
        return cls(
            matrix_id=d['matrix_id']
            template=d['template']
            job_desc=d.get('job_desc')
            tailor=d['tailor']
            ai_enhance=d['ai_enhance']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'matrix_id': self.matrix_id,
            'template': self.template,
            'job_desc': self.job_desc,
            'tailor': self.tailor,
            'ai_enhance': self.ai_enhance,
        }

# Response payload for resume.compile
@dataclass
class ResumeCompileResponse:
    # CAS content-addressed ID of generated PDF
    exhibit_id: str
    # Accessible HTTP URL on Pi5
    pdf_url: str
    # Compilation time in milliseconds
    duration_ms: int
    # Jaccard similarity score if tailored
    tailor_score: Optional[float] = None

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ResumeCompileResponse':
        return cls(
            exhibit_id=d['exhibit_id']
            pdf_url=d['pdf_url']
            duration_ms=d['duration_ms']
            tailor_score=d.get('tailor_score')
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'exhibit_id': self.exhibit_id,
            'pdf_url': self.pdf_url,
            'duration_ms': self.duration_ms,
            'tailor_score': self.tailor_score,
        }

# Request payload for resume.export.markdown
@dataclass
class ResumeExportMarkdownRequest:
    # ID of resume matrix to export
    matrix_id: str

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ResumeExportMarkdownRequest':
        return cls(
            matrix_id=d['matrix_id']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'matrix_id': self.matrix_id,
        }

# Response payload for resume.export.markdown
@dataclass
class ResumeExportMarkdownResponse:
    # Rendered Markdown content of the resume
    markdown: str

    @classmethod
    def from_dict(cls, d: dict[str, Any]) -> 'ResumeExportMarkdownResponse':
        return cls(
            markdown=d['markdown']
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            'markdown': self.markdown,
        }
